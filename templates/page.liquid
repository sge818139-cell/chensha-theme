{% layout 'theme' %}
<section style="padding:80px 0">
  <div class="container" style="max-width:800px">
    <h1 class="sec-title">{{ page.title }}</h1>
    <div class="sec-lead" style="margin-top:24px">{{ page.content }}</div>
  </div>
</section>
