{% layout 'theme' %}
<section style="padding:120px 60px;text-align:center">
  <div class="sec-eyebrow">404</div>
  <h1 class="sec-title">Page Not Found<span class="cn">頁 面 不 存在</span></h1>
  <p class="sec-lead" style="margin:0 auto 32px">The page you were looking for doesn't exist or has moved.</p>
  <a href="{{ routes.root_url }}" class="btn btn-gold">Back to Home</a>
</section>
